export enum Prioridade {
  URGENTE = 'urgente',
  IMPORTANTE = 'importante',
  NORMAL = 'normal'
  // eslint-disable-next-line prettier/prettier
}

export enum Status {
  PENDENTE = 'pendente',
  CONCLUIDA = 'concluída'
  // eslint-disable-next-line prettier/prettier
}
